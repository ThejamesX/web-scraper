[pytest]
asyncio_mode = auto
markers =
    slow: marks tests as slow (integration tests with real network requests)
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
